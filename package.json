{
  "scripts": {
    "publish": "npm run publish:2 && npm run publish:3",
    "publish:2": "cross-env METEOR_VERSION=2 meteor publish --release 2.16",
    "publish:3": "cross-env METEOR_VERSION=3 meteor publish --release 3.0"
  },
  "devDependencies": {
    "@danielx/civet": "^0.11.2",
    "cross-env": "^7.0.3"
  },
  "civetConfig": {
    "parseOptions": {
      "coffeeBooleans": true
    }
  }
}
